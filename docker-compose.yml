version: '3'
services:
  app:
    build: .
    container_name: darkswap-pro-fixed
    ports:
      - "3002:3002"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped        

